#!/bin/bash

if [ -z ${CIRCLE_TEST_REPORTS}];
  then echo "CIRCLE_TEST_REPORTS is not set";
  else echo "CIRCLE_TEST_REPORTS is set";
fi

# - cd ./native
# - npm install
# - npm run ci
# - mkdir -p $CIRCLE_TEST_REPORTS/xunit
# - mkdir -p $CIRCLE_TEST_REPORTS/junit
# - cp .report.xunit.xml $CIRCLE_TEST_REPORTS/xunit