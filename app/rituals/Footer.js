import React, {
  Text,
  View,
} from 'react-native';
import {styles} from '../styles';

const Footer = () => (
  <View style={styles.centeredBox}>
    <Text style={styles.greyBodyText}>Footer</Text>
  </View>
);

export default Footer;
