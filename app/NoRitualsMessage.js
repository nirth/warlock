import React, {
  View,
} from 'react-native';
import {
  Message,
} from './components';

const NoRitualsMessage = () => (
  <View>
    <Message>app.noRituals</Message>
  </View>
);

export default NoRitualsMessage;
